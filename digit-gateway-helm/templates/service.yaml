apiVersion: v1
kind: Service
metadata:
  name: {{ include "digit-gateway-helm.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "digit-gateway-helm.name" . }}
    helm.sh/chart: {{ include "digit-gateway-helm.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "digit-gateway-helm.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
